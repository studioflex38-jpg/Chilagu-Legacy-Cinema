name: chilagu_legacy_cinema
description: Chilagu Legacy Cinema Pro
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true